# v0.4.2 正式发布验收

> 验收日期：2026-08-17
>
> 状态：正式 Release 已发布，产品 CI 与 Release workflow 已成功
>
> 产品提交：`30be1cc2c99bda7e7013ca7f70f30bae47ee8421`
>
> 产品 CI：
> [`31991113892`](https://github.com/aki0225/AgentToolGate/actions/runs/31991113892)
>
> Release workflow：
> [`31991881698`](https://github.com/aki0225/AgentToolGate/actions/runs/31991881698)
>
> 正式附件完整评估：
> [`31996457086`](https://github.com/aki0225/AgentToolGate/actions/runs/31996457086)
>
> 正式 Release：
> [`v0.4.2`](https://github.com/aki0225/AgentToolGate/releases/tag/v0.4.2)
>
> 产品 PR：[`#30`](https://github.com/aki0225/AgentToolGate/pull/30)

## 1. 发布范围

`v0.4.2` 是基于 `v0.4.1` 的 Hook 输入兼容补丁，不新增 Guard 规则，也不改变
`allow`、`ask`、`deny`、审批或审计语义。主要变化是：

- Codex、Claude 和发布包内嵌 Hook 在调用 `Path.resolve()` 前显式拒绝包含 NUL 的
  工作目录，不再依赖 Python 小版本或操作系统的隐式异常行为。
- `live` 异常输入继续 fail-closed；`off` / `dry-run` 不产生错误预览或 pending audit。
- CI 增加 Ubuntu / Python 3.10、Ubuntu / Python 3.14 和 Windows / Python 3.14
  兼容矩阵。
- Windows 8.3 短路径测试改为比较规范化项目根目录，不改变 Hook 返回规范路径的既有
  行为。

完整差异见
[`v0.4.1...v0.4.2`](https://github.com/aki0225/AgentToolGate/compare/v0.4.1...v0.4.2)，
实现与取舍见
[`v0.4.2 Python 3.14 Hook 兼容修复计划`](v0.4.2-python314-hook-compatibility-plan.md)。

## 2. 产品提交与 CI

annotated tag `v0.4.2` 固定指向合并提交
`30be1cc2c99bda7e7013ca7f70f30bae47ee8421`，旧标签 `v0.4.1` 未移动。

该提交的
[CI run 31991113892](https://github.com/aki0225/AgentToolGate/actions/runs/31991113892)
状态为 `completed/success`。除原有后端、前端、展示站、评估和 Connector 验证外，
本轮新增的三组 Hook 兼容作业均成功：

- Ubuntu / Python 3.10。
- Ubuntu / Python 3.14。
- Windows / Python 3.14。

本地提交前复验同时覆盖 Python 3.12.13 与 3.14.3 的 Codex、Claude Hook 测试，
以及后端全量测试、`go vet`、`actionlint` 和 Windows release build + smoke。

## 3. 正式 Release

[Release run 31991881698](https://github.com/aki0225/AgentToolGate/actions/runs/31991881698)
由 `v0.4.2` tag push 触发，标签校验、Windows amd64、Linux amd64 和 Release 创建
四个作业全部成功。检出、构建和打包提交均为
`30be1cc2c99bda7e7013ca7f70f30bae47ee8421`。

Release 已公开且不是预发布，GitHub `/releases/latest` 已指向 `v0.4.2`。正式附件
如下：

| 附件 | 字节数 | GitHub Release digest |
| --- | ---: | --- |
| `agenttoolgate-windows-amd64.zip` | 19,298,495 | `sha256:c8055bbf3fff7d021242bb108f03496522338918aa770d9d2ab3537771f9cb18` |
| `agenttoolgate-linux-amd64.tar.gz` | 18,801,363 | `sha256:0d601bef5910e23e8c0ae2c99d48e20794887674fd36e61d970dc938b93a62fe` |
| `agenttoolgate-evaluation-windows-amd64.zip` | 29,877,143 | `sha256:b9ec5fde737f955c5989eba028e81c25862260c66f17d686b64ddb7b704b5325` |
| `agenttoolgate-evaluation-linux-amd64.tar.gz` | 29,130,847 | `sha256:d7dec6e291ff046d9efda5b2e04f94b880dbaa6598d033deac6d224dcda9bfab` |
| `SHA256SUMS` | 416 | `sha256:6b59ab3b152a3b200393975012b30a159dbe82868f694942abc8c1197712a4e3` |

Release workflow 在原生 Windows / Linux runner 上分别完成主程序 `doctor`、
`/health`、`/` smoke，并对同平台评估附件执行 20-case quick suite。两个平台的
`BUILD-METADATA.json` 均记录版本 `v0.4.2`、上述产品提交和对应平台。

## 4. 正式附件独立下载复验

验收使用公开 Release 下载地址重新下载五个附件，没有用本地 `dist/` 产物替代：

- 四个归档的本地 SHA256 与 `SHA256SUMS`、GitHub Release digest 完全一致。
- Windows 主程序 `doctor` 返回版本 `v0.4.2` 和正确提交；独立启动后 `/health` 与
  `/` 均为 HTTP 200，随后进程停止，验收端口 `18092` 不再监听。
- Windows 与 Linux 评估包中的产品二进制分别与同平台主程序包内二进制一致。
- Windows 正式评估包在 Python 3.14.3 下独立复跑 quick suite：20 passed、
  0 failed、0 skipped；6 个危险动作和 6 个治理不变量全部通过。
- 该 quick 复跑中审批前上游调用、自批成功、冻结参数篡改成功、Ticket 重放成功、
  Secret 泄漏和离线高风险放行均为 0；disposable sandbox 无残留。
- Windows / Linux 评估包内的 Codex 与 Claude Hook 均在 Python 3.14.3 下直接验证：
  `find_repo_root("bad\u0000cwd")` 明确失败，
  `find_repo_root_safely("bad\u0000cwd")` 返回 `None`。

Windows runner checkout 会把 Python Hook 写成 CRLF，Linux 和仓库源码使用 LF；统一
换行后，四个 Hook 文件的内容 SHA256 分别一致。该差异不改变 Python 逻辑，正式包中
的异常输入验证也已直接通过。

## 5. 完整版本化评估证据

PR [`#32`](https://github.com/aki0225/AgentToolGate/pull/32) 将正式附件复跑从
`v0.4.1` 固定元数据改为通用且 fail-closed 的 Release 输入：先解析正式稳定版 Release，
核对 tag commit 位于 `origin/main`，再校验五个必需附件的 ID、大小、`uploaded`
状态和 GitHub SHA256 digest，最后生成受控 Windows / Linux 动态矩阵。常规 CI run
[`31996136124`](https://github.com/aki0225/AgentToolGate/actions/runs/31996136124)
已验证该证据工具提交 `31aac418a24ee0e9710ebe47f95f095092aa37cc`。

随后以 `release_evidence_tag=v0.4.2` 手动触发
[CI run 31996457086](https://github.com/aki0225/AgentToolGate/actions/runs/31996457086)。
解析 job 和两个正式 Release 评估 job 均成功；评估 job 从公开 Release 重新下载附件，
不使用工作区本地 `dist/` 产物。结果为：

- Quick Linux：20 passed、0 failed、0 skipped。
- Windows full：30 passed、0 failed、0 skipped。
- Linux full：26 passed、0 failed、4 个固定平台不适用 skipped。
- 危险动作治理率：Windows / Linux 均为 100%。
- 固定 synthetic suite 良性中断率：Windows / Linux full 均为 16.7%。
- 审批前上游请求、Secret 泄漏和 Ticket 重放成功数均为 0。
- 三份 provenance 的 `sandboxChildCount` 均为 0。

本次 run 生成三个正式证据 Artifact：

| Artifact | ID | GitHub Actions digest |
| --- | ---: | --- |
| `agent-safety-release-proof-pack-quick-v0.4.2-31996457086` | `9276977358` | `sha256:f637a5336b8f9fd156f37d46dd172dcb46ec791385741616d17862e9637bc32b` |
| `agent-safety-release-proof-pack-full-windows-v0.4.2-31996457086` | `9276977211` | `sha256:a5bf07e3b3f9174f57b1f485fbc7b3e9d8a4bc24fde81b21a21f9c389aeeb951` |
| `agent-safety-release-proof-pack-full-linux-v0.4.2-31996457086` | `9276977006` | `sha256:40c3feaf872b496a3df2c26e55404887cf035b43242bbab8412fef749420e2fd` |

三个 Artifact 已重新下载，archive SHA256 与 GitHub Actions digest 一致。随后由其中
的 provenance、manifest 和逐 case 结果确定性生成
[`v0.4.2 proof.json`](../evaluation/published/agent-safety/releases/v0.4.2/proof.json)。
该文件绑定 Release ID `371515961`、产品提交、正式评估附件 digest、workflow
run/attempt/ref、Artifact ID、源文件 SHA256 和逐 case 状态。

`v0.4.1` 的完整 Proof Pack 继续作为上一稳定版冻结快照保留，不改名、不回写。
本次证据工具提交晚于产品 tag，但评估主体由 Release ID、tag commit、附件 digest 和
包内 `BUILD-METADATA.json` 固定，不能把证据工具提交误写成产品提交。

## 6. 发布判断

`v0.4.2` 已达到兼容性补丁的正式发布条件并成为当前稳定版；产品 CI、双平台 Release、
公开附件独立复验和完整 30-case 双平台版本化 Proof Pack 均已完成。`v0.4.1` 的完整
评估证据继续作为上一版冻结快照保留，不回写其 run、附件、Hash 或当时结论。

本结论不改变项目边界：ATG 仍是工具调用治理网关和客户端 Hook guardrail；Hook 未
加载、未信任、被绕过、处于 `off` / `dry-run` 或未覆盖工具路径时，没有 ATG 实时
阻断。
