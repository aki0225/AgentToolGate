# v0.4.0 正式发布验收

> 历史快照：本页记录 `v0.4.0` 发布时的事实；当前稳定版见
> [`v0.4.1 发布验收`](v0.4.1-release-acceptance.md)。
>
> 验收日期：2026-08-16
>
> 状态：正式 Release 已发布，附件已独立下载验证
>
> 产品提交：`a059e9f2c9c97fe2afb0cbceead710d623799b5c`
>
> 产品 CI：
> [`31934019158`](https://github.com/aki0225/AgentToolGate/actions/runs/31934019158)
>
> Release workflow：
> [`31941172465`](https://github.com/aki0225/AgentToolGate/actions/runs/31941172465)
>
> 正式 Release：
> [`v0.4.0`](https://github.com/aki0225/AgentToolGate/releases/tag/v0.4.0)
>
> 产品 PR：[`#22`](https://github.com/aki0225/AgentToolGate/pull/22)

## 1. 验收范围

本轮验证 `v0.4` 日常使用加固，不把它描述成 OS sandbox、完整 DLP 或企业级生产
控制。范围包括：

- Hook 生命周期、`off` / `dry-run` / `live` 和服务退出行为。
- 普通开发低干扰与高危本地动作阻断。
- Secret、HTTP SSRF、MCP inbound/outbound 和审批边界。
- 项目规则校验、解释和跨平台发布脚本。
- 前端控制台、展示站与 Windows amd64 发布包。

所有临时二进制、日志、数据库和浏览器结果都写入忽略的 `<repo>/.tmp/` 或同盘
workspace 临时目录。未执行原始危险命令；删除、敏感读取和网络外传只作为 Hook JSON
输入探针。

## 2. 自动安全评估

当前源码构建的 `agenttoolgate` 与 `atg-eval` 运行三套独立 suite：

| Suite | 结果 |
| --- | --- |
| `dangerous-actions-v1` | 12 passed / 0 failed / 0 skipped |
| `benign-development-v1` | 12 passed / 0 failed / 0 skipped |
| `governance-invariants-v1` | 6 passed / 0 failed / 0 skipped |

结果文件可严格解析，Proof Pack 完整；敏感扫描未发现仓库绝对路径、用户目录、Bearer、
PAT 或 synthetic Secret 明文。sandbox 和子进程无残留。

## 3. Hook 真实验收

在 `<repo>/.tmp/` 下创建独立嵌套 Git 项目，通过当前源码构建的二进制执行 `init all`
与动态端口 `up`：

- `off` 完全 no-op。
- `dry-run` 精确产生 4 条脱敏预览，不阻断。
- `live` 下普通 `git status`：Codex 零输出放行，Claude 返回协议允许结果。
- Codex 与 Claude 均拒绝敏感读取、根递归删除、网络外传和受保护路径写入。
- 本地 collector 请求数为 0；sentinel、HEAD、tree 和目标文件 Hash 均不变。
- 服务异常停止后，高风险输入 fail closed；两个明确低风险动作进入 pending audit。
- 最终 control 恢复 `off`，endpoint/executable 清空，后台服务停止。

100 个 warm 样本下，Codex 与 Claude 的 live p95 均低于 250ms；普通开发样本无失败或
超时。

## 4. Secret、HTTP 与 MCP

聚焦测试和源码复核确认：

- resolved Secret 在前后字符拼接、嵌套 JSON 和重叠值中仍会精确脱敏。
- HTTP / MCP 出站按请求和 redirect 重新解析 DNS，拒绝 metadata、link-local 和
  非显式 loopback，并固定拨号到本次校验 IP。
- HTTP 重定向只允许同 scheme、同规范化 authority。即使两个 authority 都在
  allowlist 中，跨 Origin 跳转也会在第二个服务收到请求前拒绝，避免
  `headerSecretRefs` 解析出的 Secret Header 被转发。
- HTTP / MCP 出站禁用环境代理。
- MCP outbound 限制 SSE 单行 64 KiB；SSE event、POST request/response 和 JSON-RPC
  result 各 1 MiB；最多 256 tools，单个 input schema 64 KiB。
- MCP inbound 请求和响应限制为 1 MiB，拒绝尾随 JSON。
- MCP outbound 响应必须匹配 JSON-RPC 版本与请求 ID，且 result/error 二选一。
- 工具参数错误使用显式类型映射，不按错误字符串关键词决定是否公开。

边界仍然存在：显式 allowlist authority 可以解析到普通 RFC1918、IPv6 ULA 或 CGNAT
私网地址。当前实现不是完整私网隔离；若部署同时访问公网和内部服务，应在外部网络策略
或后续目标分区配置中继续限制。

## 5. 本地质量门禁

已通过：

- `go test -count=1 -timeout 60s ./...`
- `go vet ./...`
- `scripts/tests/local-cache-env.tests.ps1`：5 项通过
- PowerShell 脚本 AST 解析
- `scripts/verify-local.ps1 -WithE2E`
- frontend `npm run check`
- frontend `npm run build`
- frontend CI 对齐核心 Playwright：40 passed
- website `npm run check`
- website `npm test`：36 passed，覆盖率门禁通过
- website `npm run build`
- website Playwright：8 passed
- `scripts/build-release.ps1 -Platform windows-amd64` 完整 smoke
- `git diff --check`

产品提交 `a059e9f2c9c97fe2afb0cbceead710d623799b5c` 的
[CI run 31934019158](https://github.com/aki0225/AgentToolGate/actions/runs/31934019158)
已全部成功，覆盖 Linux/Windows Go、PostgreSQL、多 Actor、Agent Safety Evaluation、
前端、展示站和 Connector smoke。Windows job 还实际执行了
`local-cache-env.tests.ps1` 与 `verify-local-postgres.tests.ps1`。

前端生产构建仍有已知 chunk size warning；它不影响本轮正确性，但后续可通过路由级
动态加载单独优化。

## 6. 正式 Release 验收

annotated tag `v0.4.0` 固定指向产品提交
`a059e9f2c9c97fe2afb0cbceead710d623799b5c`。最终
[Release run 31941172465](https://github.com/aki0225/AgentToolGate/actions/runs/31941172465)
通过 `workflow_dispatch` 显式传入该 tag 和完整 commit SHA；workflow 虽从更新后的
`main` 运行，但检出、构建和打包的仍是产品提交。

`PR #23` 和 `PR #24` 只修复 Release 草稿列表短暂一致性与 PowerShell 上传 URI
解析，不移动标签、不覆盖同标签 Release，也不改变产品二进制。

正式附件独立下载后的结果：

| 附件 | 字节数 | SHA256 |
| --- | ---: | --- |
| `agenttoolgate-windows-amd64.zip` | 19,297,761 | `abf68d05419fb67a036b9d729c2c5048bc69c7536b90048bc1ad3bd8eb20a096` |
| `agenttoolgate-linux-amd64.tar.gz` | 18,800,787 | `87a8bae8ad00e51680e99590c3568910f9a2f4b78320285a0f306701f6310ff3` |
| `agenttoolgate-evaluation-windows-amd64.zip` | 29,875,545 | `1c1c9192a244d972c2295c67f95a86c3c6d6cd1a57b70ec1fe0bf0e035210c4a` |
| `agenttoolgate-evaluation-linux-amd64.tar.gz` | 29,128,031 | `7d6c826cc14048da0851fb0c2f94c36d12cedd17f572a256bf447d5ad0e28c11` |
| `SHA256SUMS` | 416 | `3c37e9b1de9a01f203686b39f5b92bbfce05da368a0c089e488659ada22c498d` |

四个归档的实际 SHA256 同时匹配 `SHA256SUMS` 和 GitHub Release digest，归档目录均可
读取。Release 元数据为 `draft=false`、`prerelease=false`，目标提交为产品提交；
`releases/latest` 已指向 `v0.4.0`。

本机未以 `TEST_DATABASE_URL` 单独重跑完整 PostgreSQL 集成路径；远端
`backend-postgres` 与 `multi-actor-postgres` jobs 已在产品 CI 中实际执行并通过。

## 7. 发布判断与边界

`v0.4.0` 已达到正式发布条件，当前稳定版由 `v0.3.2` 更新为 `v0.4.0`。本结论证明
产品提交、CI、双平台构建、发布附件和校验链路一致，不改变项目边界：ATG 仍是
guardrail，不是 OS sandbox、完整 DLP 或企业级生产访问控制。
