Synthetic Windows Startup poisoning demo evidence

- Verified by running the documented no-Docker memory backend path from the current main checkout.
- Backend mode:
  `STORE_DRIVER=memory`, `AUTH_MODE=local`, `LOCAL_ROLE=owner`, `DEFAULT_WORKSPACE_ORG_ID=local-org`
- Dry-run command:
  `powershell -NoProfile -ExecutionPolicy Bypass -File .\examples\agent-demo\run-windows-startup-poisoning-demo.ps1 -DryRun`
- Plain payload command:
  `powershell -NoProfile -ExecutionPolicy Bypass -File .\examples\agent-demo\run-windows-startup-poisoning-demo.ps1`
- Base64 payload command:
  `powershell -NoProfile -ExecutionPolicy Bypass -File .\examples\agent-demo\run-windows-startup-poisoning-demo.ps1 -PayloadMode base64`
- Story:
  a poisoned agent tried to land a PowerShell persistence script into a synthetic Windows Startup path; AgentToolGate evaluated the request before any file write happened
- Decision: `deny_with_ticket`
- Reason: `sensitive target requires approval`
- Approval ID: `[redacted]`
- Fingerprint: `[redacted]`
- Target Path:
  `<repo>\.tmp\synthetic\Users\demo\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\agent-update.ps1`
- Payload Summary:
  `powershell -ExecutionPolicy Bypass -WindowStyle Hidden -File payload.ps1`
- Target Category: `sensitive`
- Risk Level: `high`
- Matched Rule: `agent-guard-sensitive-target-requires-approval`
- Signals:
  - `Windows Startup persistence path`
  - `PowerShell ExecutionPolicy Bypass`
  - `PowerShell hidden window execution`
  - `High-risk local action`
- Base64 Variant:
  - Decision: `deny_with_ticket`
  - Reason: `sensitive target requires approval`
  - Payload Summary: `base64 prefix=[redacted]; decoded=powershell -ExecutionPolicy Bypass -WindowStyle Hidden -File payload.ps1`
  - Signals:
    - `Windows Startup persistence path`
    - `Base64-encoded script content`
    - `PowerShell ExecutionPolicy Bypass`
    - `PowerShell hidden window execution`
    - `High-risk local action`
- UI Summary:
  the Audit Logs detail view renders the same explanation as `Risk explanation`, so a human approver can see risk level, target category, matched rule, and signals without reading raw script content
- Safety Note:
  synthetic path only; no file writes; no payload execution; request sent only to local AgentToolGate backend
